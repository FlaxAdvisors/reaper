{
  "log-level": "warn",
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "5"
  },
  "selinux-enabled": true,
  "bip": "{{ apply_docker_bip }}",
  "default-address-pools": [
    {"base": "{{ apply_docker_pool_base }}", "size": {{ apply_docker_pool_size }}}
  ]
}
