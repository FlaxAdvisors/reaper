# Caps coredump storage so a flapping daemon (e.g. the bang-gouda
# dnsmasq SIGABRT recurrence) doesn't fill /var. systemd-coredump
# rotates oldest-first when MaxUse is hit.
[Coredump]
MaxUse=2G
KeepFree=1G
Compress=yes
