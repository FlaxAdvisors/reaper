# Rendered per-site into post.tgz by apply_pxe_payloads; loaded by the flax-qual-agent
# systemd unit (post.sh: systemd-run --property=EnvironmentFile=). Site-specific runtime
# config for the Wave-4 qualification agent -- NO secrets here (this file ships in the
# tarball in cleartext; secrets stay in setipmiuser.sh rendered from the vault template).
FLAX_IPERF_SERVER={{ post_iperf_server }}
