# Managed by hand. No nodes assigned to post data vlans yet at
# eindhoven. Add per-vid sections when nodes arrive (mirror the
# braintree shape: header + per-vid section comments + dhcp-host
# lines using the rabbit Et<port>/<breakout> IP scheme).
#
# Infrastructure slots (do not assign to DUTs):
#   .1 = bang-gouda, .2 = bang-edam, .3 = rabbit-gouda,
#   .4 = rabbit-edam, .5 = turtle-gouda.
# Behind-turtle DUTs (172.<vid>.0.<turtle_swpN> scheme) should pick swp6+
# to avoid IP collision with the .1-.5 infra slots.
